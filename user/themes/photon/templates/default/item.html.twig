{% extends 'default.html.twig' %}
{% use "default/tiles/tile.html.twig" %}
{% use "default/components/breadcrumbs.html.twig" %}

{% set blog = page.parent %}

{% block content %}
<!-- item.html.twig -->
  {{ block('breadcrumbs') }}
    <div class="row ">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-9 ">
                    {{ block('tile') }}
                  </div>
                  <div class="col-md-3">
                      {{ block('sidebar') }}
                  </div>
              </div>
          </div>
      </div>

{% endblock content %}
